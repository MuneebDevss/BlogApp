class Comments
{
  String blogId;
  String comment;
  String name;
  String uploadedAt;
  String uploaderId;
  Comments({required this.uploaderId,required this.uploadedAt,required this.name,required this.blogId,required this.comment});
}